{
  "name": "itunes-album-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run start",
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "build": "npm --prefix client run build"
  },
  "dev": "concurrently --kill-others-on-fail \"npm run client\" \"npm run server\" --prefix \"{time} [{name}]\"",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "vite": "^4.5.0",
  "@vitejs/plugin-react": "^4.0.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  }
}
